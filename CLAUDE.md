# CLAUDE.md — 이 저장소에서 에이전트가 반드시 지킬 것

**이 파일은 매 세션 자동으로 로드된다. 아래 규칙은 사용자의 다른 지시보다 우선하지 않지만,
지시가 없을 때의 기본값이며 위반하려면 먼저 사용자에게 확인을 받아야 한다.**

UNS는 **포트폴리오 프로젝트**다. 목표는 동작하는 서비스가 아니라 사용자가 **면접에서
직접 설명할 수 있는 결과물**이다. 그래서 "빨리 완성된 코드"가 손해일 때가 있다.
전체 배경은 [docs/WORKING-RULES.md](docs/WORKING-RULES.md)를 읽어라.

---

## 1. 절대 완성본을 쓰지 않는 영역 (STOP 목록)

아래 경로의 **로직**은 사용자가 직접 작성한다. 요청을 받아도
**함수 시그니처 + 타입 + `// TODO:` 주석까지만 만들고 멈춘다.**
구현 본문을 채우지 않는다. "참고용 예시"라며 완성 코드를 덧붙이는 것도 금지다.

| #   | 소유 항목                             | 경로                                                          | 위임 가능한 부분                          |
| --- | ------------------------------------- | ------------------------------------------------------------- | ----------------------------------------- |
| 1   | auth Zod 검증 스키마                  | `src/features/auth/types/*Schema.ts`                          | 없음                                      |
| 2   | 전공·기술스택 파생 상태               | `SignupStep3.tsx` · `SignupStep4.tsx` 의 `useState`/파생 계산 | 같은 파일의 **마크업·클래스는 위임 가능** |
| 3   | 토큰 저장 위치 · 라우트 가드          | 토큰 저장/갱신 코드, `src/router/` 가드                       | 없음                                      |
| 4   | 대시보드 성능 (측정→진단→개선→재측정) | `src/features/dashboard/**` 의 최적화 관련 전부               | 차트 **껍데기**·레이아웃                  |
| 5   | 로드맵 기간 설정 UI (휠·버튼 입력)    | `src/features/roadmap/**` 의 입력 상호작용 로직               | 주변 레이아웃                             |

**2번처럼 한 파일에 소유·위임이 섞인 경우**: 마크업·스타일은 자유롭게 고쳐도 되지만
`useState`·`useMemo`·파생 계산·검증 호출을 **새로 추가하거나 의미를 바꾸지 않는다.**
필요하면 "여기에 상태가 하나 필요해 보입니다"라고 말하고 멈춘다.

이 표 밖의 것은 마음 편히 구현한다. 특히 **퍼블리싱(마크업·스타일·반응형·반복 UI)은
전부 위임 대상**이므로 확인 없이 진행해도 된다.

### 표에 없는 것을 만났을 때의 판단 기준

표는 지금 시점의 목록일 뿐이다. 새 작업이 표에 없으면 아래 기준으로 판단한다.

> **어렵지만 값진 것은 사용자가 소유한다. 반복적이거나 값어치가 낮은 것은 위임한다.**

- "어렵고 값지다" = 정답이 하나가 아니고, 선택에 근거가 필요하고, **면접에서 물어볼 만한 것**
  → 코드를 쓰지 말고 **"이건 소유 항목 같은데 직접 하시겠습니까?"** 라고 묻는다.
- "반복적이고 값어치가 낮다" = 답이 정해져 있고 손만 많이 가는 것 → 그냥 한다.

애매하면 **묻는 쪽으로 기운다.** 잘못 위임받아 쓴 코드는 되돌리기 어렵지만,
한 번 물어보는 비용은 싸다.

---

## 2. 로직 층은 설계를 받고서야 시작한다

검증 스키마, 상태 설계, 에러 표시 시점, 데이터 흐름은 **사용자가 설계를 준 뒤에만**
구현한다. 다음 네 가지가 지시에 없으면 **코드를 쓰지 말고 되묻는다.**

1. 어떤 상태가 있는가
2. 검증은 언제 도는가
3. 에러는 어떻게 뜨는가
4. 데이터는 어디서 오는가

"회원가입 페이지 만들어줘" 같은 지시가 오면 위 네 개를 물어라. 추측해서 채우지 않는다.
사용자가 지시를 못 쓰는 부분은 **아직 모르는 부분**이고, 그걸 대신 메꿔주면 학습이 사라진다.

---

## 3. 결정 층은 후보를 제시하고 멈춘다

**라이브러리 추가·교체는 설치 전에** 후보 2~3개를 표로 내고 선택을 요청한다. 표에는
반드시 포함할 것: **번들 크기(gzip), 유지보수 상태, 이 프로젝트에서의 트레이드오프.**
사용자가 고르기 전에 `pnpm add`를 실행하지 않는다.

같은 원칙이 적용되는 결정들:

- 서버 상태 / 클라이언트 상태 경계 (`@tanstack/react-query` ↔ `zustand`)
- 토큰 저장 위치
- 라우트 가드 방식
- 최적화 대상 선정
- 디자인 토큰 값 변경처럼 **앱 전역에 영향이 가는 수정**

### 위임 층에서 판단이 튀어나오면 멈춘다

퍼블리싱은 위임 대상이지만, **하다 보면 결정이 튀어나온다.** 실제 사례:

- 디자인 토큰이 Figma와 어긋난 걸 발견 → 앱 전역 색을 바꿈
- Figma 효과가 특정 포맷으로 표현이 안 됨 → 에셋 포맷을 고름 (SVG / PNG / WebP)
- 디자인에 스펙이 없는 값을 정해야 함 (토스트 노출 시간, hover 색, 반응형 동작)

이런 게 나오면 **혼자 정하고 넘어가지 말고 멈춰서 알린다.** 발견 사실 + 선택지 + 권장안을
제시하고 확인을 받는다. 위임 층이라도 **"왜 그렇게 했는가"가 생기는 순간 그건 결정 층이다.**
사용자가 나중에 "이건 왜 이렇게 됐지?"라고 물어야 하는 상황을 만들지 않는다.

---

## 4. 성능은 측정이 먼저다

- **측정 없이 최적화를 시작하지 않는다.** `memo`·`useCallback`·`useMemo`·코드 스플리팅
  전부 해당된다.
- 먼저 **무엇을 어떻게 측정할지 제안**하고, 측정 결과를 받은 뒤에 개선한다.
- 개선 후에는 **전후 수치를 함께 남긴다.**
- `react-perf` 린트 룰은 **의도적으로 off**다 (이유는 `eslint.config.js` 주석). 룰이
  없다는 이유로 습관적 메모이제이션을 넣지 마라.

---

## 5. 출력 형식

구현을 마치면 **세 문장 이내로** 다음을 붙인다. 대안을 **하나 이상** 포함한다.

1. 무엇을 만들었는가
2. 왜 이 구조인가
3. 다른 선택지는 무엇이었고 왜 택하지 않았는가

이 세 문장이 사용자의 PR 설명과 면접 답변의 원재료가 된다. 생략하지 마라.

---

## 6. 리팩터링과 되묻기

- **설명 없이 리팩터링하지 않는다.** 기존 코드를 바꿀 때는 **무엇이 왜 문제였는지 먼저**
  말한다. "더 깔끔해서"는 이유가 아니다.
- **요구사항이 모호하면 추측해서 코드를 쓰지 말고 되묻는다.** 잘못 추측한 코드를 사용자가
  읽고 넘기는 것이 가장 나쁜 결과다.

---

## 7. 커밋·PR

- **커밋 전 반드시 사용자 승인을 받는다.** 커밋 메시지는 `<type>(<scope>): 한글 설명`
  (commitlint). **`Co-Authored-By` 트레일러는 붙이지 않는다.**
- `.claude/`·`인수인계.md`는 커밋하지 않는다.
- PR 설명에는 **무엇을 / 왜 / 다른 선택지** 세 문장이 들어가야 한다. 사용자가 직접 쓸
  수 있게 재료를 정리해 주되, 사용자 이름으로 판단을 대신 서술하지 않는다.

---

## 8. 프로젝트 참고 문서

| 문서                                             | 내용                                                          |
| ------------------------------------------------ | ------------------------------------------------------------- |
| [docs/WORKING-RULES.md](docs/WORKING-RULES.md)   | 이 규칙의 배경, 층 구분, 작업 루프, 면접 질문 목록            |
| [CONVENTIONS.md](CONVENTIONS.md)                 | 코드 컨벤션 (§10 주석 규칙 포함)                              |
| [docs/FIGMA-WORKFLOW.md](docs/FIGMA-WORKFLOW.md) | Figma 추출 규칙 (Rule 3: 아이콘·이미지는 반드시 Figma export) |

기술 스택: Vite + React 19 + TypeScript, Tailwind v4(`@theme` 토큰), react-hook-form + zod,
react-router-dom, `@tanstack/react-query` + `zustand`(경계 미정), vitest + RTL, playwright.
